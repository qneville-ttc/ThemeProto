<%= form_for(@theme) do |f| %>
  <% if @theme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theme.errors.count, "error") %> prohibited this theme from being saved:</h2>

      <ul>
      <% @theme.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :theme_name %><br />
    <%= f.text_field :theme_name %>
  </div>
  <div class="field">
    <%= f.label :theme_desc %><br />
    <%= f.text_area :theme_desc %>
  </div>
  <div class="field">
    <%= f.label :career_site_id %><br />
    <%= f.number_field :career_site_id %>
  </div>
  <div class="field">
    <%= f.label :theme_filename %><br />
    <%= f.text_field :theme_filename %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
