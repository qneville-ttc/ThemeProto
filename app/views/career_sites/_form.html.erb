<%= form_for(@career_site) do |f| %>
  <% if @career_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@career_site.errors.count, "error") %> prohibited this career_site from being saved:</h2>

      <ul>
      <% @career_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
